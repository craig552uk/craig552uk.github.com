<div>
	<h1 class="text-4xl font-bold mb-4">All Tags</h1>

	<ul>
		{% for tag in collections | getKeys | filterTagList %}
			{% set tagUrl %}/tags/{{ tag | slugify }}/{% endset %}
			<li>
				<a href="{{ tagUrl }}" class="hover:underline">{{ tag }}</a>
			</li>
		{% endfor %}
	</ul>
</div>